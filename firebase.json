{
  "firestore": {
    "rules": "firestore.rules"
  },
  "storage": {
    "rules": "storage.rules"
  },
  "functions": {
    "source": "functions",
    "runtime": "nodejs20",
    "predeploy": [
      "cd \"$RESOURCE_DIR/tripshistory\" && npm install --no-audit --no-fund && npm run build"
    ]
  }
}
